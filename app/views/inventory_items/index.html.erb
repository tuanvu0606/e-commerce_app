<p id="notice"><%= notice %></p>

<h1>Inventory Items</h1>

<table>
  <thead>
    <tr>
      <th>Inv item category</th>
      <th>Model</th>
      <th>Name</th>
      <th>Price</th>
      <th>Description</th>
      <th>Warehoused on</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @inventory_items.each do |inventory_item| %>
      <tr>
        <td><%= inventory_item.inv_item_category_id %></td>
        <td><%= inventory_item.model_id %></td>
        <td><%= inventory_item.name %></td>
        <td><%= inventory_item.price %></td>
        <td><%= inventory_item.description %></td>
        <td><%= inventory_item.warehoused_on %></td>
        <td><%= link_to 'Show', inventory_item %></td>
        <td><%= link_to 'Edit', edit_inventory_item_path(inventory_item) %></td>
        <td><%= link_to 'Destroy', inventory_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Inventory Item', new_inventory_item_path %>
